// import { createQueryKeyStore } from '@lukemorales/query-key-factory';

// export const queryKeys = createQueryKeyStore({
//   product: {
//     list: (limit) => ['products', { limit }],
//     detail: (productId) => ['product', productId],
//   },
// });
